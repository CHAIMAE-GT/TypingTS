// CustomTextPreview.js

import React from 'react';

const CustomTextPreview = ({ text }) => {
  return (
    <div>
      <h2>Preview</h2>
      <p>{text}</p>
    </div>
  );
};

export default CustomTextPreview;
